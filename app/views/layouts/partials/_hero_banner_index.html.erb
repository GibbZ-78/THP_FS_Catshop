<section class="row">
  <div class="px-4 py-5 my-5 text-center">
    <%= image_tag("banner_index", alt:"Photo d'un chaton hyper mignon qui trottine dans l'herbe", class:"d-block mx-auto mb-4") %>
    <h1 class="display-5 fw-bold">KITTEN CAT (nom non-définitif)</h1>
    <div class="col-lg-6 mx-auto">
      <p class="lead mb-4">Retrouvez des clichés HD des chatons les plus craquants du Web !</p>
      <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
        <% if !user_signed_in? # The button below will appear only if current_user is NOT connected %>
        <%= link_to("Créez votre compte",root_path,class:"btn btn-primary btn-lg px-4 gap-3") %>
        <% end %>
        <%= link_to("Parcourez notre catalogue",root_path,class:"btn btn-outline-secondary btn-lg px-4") %>
      </div>
    </div>
  </div>
</section>